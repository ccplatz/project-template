# Deployment Environments

<!-- TODO: Document the deployment setup. Keep this file even for dev-only projects.

Sections to include:
- Environment overview (dev, staging, prod)
- How to start each environment (exact commands)
- Compose files and their differences
- Port mappings
- External dependencies (networks, volumes, services)
- Environment variables per environment
- Access URLs and credentials (non-sensitive only)

Use this as the single source of truth for how to run the project.
-->
